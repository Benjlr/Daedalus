<UserControl x:Class="Icarus.Views.StrategyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:oxy="http://oxyplot.org/wpf"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:dc="clr-namespace:Icarus.ViewModels"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:Icarus.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

        <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="90*"></RowDefinition> 
            <RowDefinition Height="10*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90*"></ColumnDefinition>
            <ColumnDefinition Width="20*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <oxy:PlotView Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Model="{Binding MyResults}"  />

        <views:StatsView x:Name="Stats" DataContext="{Binding Stats}" Grid.Column="1" Grid.Row="0"></views:StatsView>

        <Button Grid.Row="1" Grid.Column="1" Command="{Binding ClickCommand}"></Button>



    </Grid>
</UserControl>
